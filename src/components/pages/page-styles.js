import { css } from 'lit';

export const pageStyles = css`
    :host {
        --dile-icon-color: orange;
        --dile-icon-size: 6rem;
    }
    h2 {
        font-weight: 300;
        margin-top: 0.2rem;
    }
`
